---
# Generated by: flux bootstrap github \
#   --owner=kvarnberg-labs \
#   --repository=hermes-coach \
#   --branch=main \
#   --path=clusters/coach \
#   --personal
# Replace with actual generated content after running flux bootstrap.
# Placeholder so the directory is tracked in git.
apiVersion: source.toolkit.fluxcd.io/v1
kind: GitRepository
metadata:
  name: flux-system
  namespace: flux-system
spec:
  interval: 1m0s
  ref:
    branch: main
  secretRef:
    name: flux-system
  url: ssh://git@github.com/kvarnberg-labs/hermes-coach
---
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: flux-system
  namespace: flux-system
spec:
  interval: 10m0s
  path: ./clusters/coach
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
